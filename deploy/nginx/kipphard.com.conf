# Cloudflare sits in front — trust their IPs so access logs record real client IPs.
set_real_ip_from 173.245.48.0/20;
set_real_ip_from 103.21.244.0/22;
set_real_ip_from 103.22.200.0/22;
set_real_ip_from 103.31.4.0/22;
set_real_ip_from 141.101.64.0/18;
set_real_ip_from 108.162.192.0/18;
set_real_ip_from 190.93.240.0/20;
set_real_ip_from 188.114.96.0/20;
set_real_ip_from 197.234.240.0/22;
set_real_ip_from 198.41.128.0/17;
set_real_ip_from 162.158.0.0/15;
set_real_ip_from 104.16.0.0/13;
set_real_ip_from 104.24.0.0/14;
set_real_ip_from 172.64.0.0/13;
set_real_ip_from 131.0.72.0/22;
real_ip_header CF-Connecting-IP;

server {
    listen 80;
    listen 443 ssl http2;
    server_name www.kipphard.com;

    # Cloudflare Origin Certificate — trusted by CF's edge only, not browsers.
    ssl_certificate     /etc/ssl/kipphard.com/origin.crt;
    ssl_certificate_key /etc/ssl/kipphard.com/origin.key;

    access_log /var/log/nginx/kipphard.com.access.log;
    error_log  /var/log/nginx/kipphard.com.error.log;

    return 301 https://kipphard.com$request_uri;
}

server {
    listen 80;
    listen 443 ssl http2;
    server_name kipphard.com;

    ssl_certificate     /etc/ssl/kipphard.com/origin.crt;
    ssl_certificate_key /etc/ssl/kipphard.com/origin.key;

    root /var/www/kipphard.com;
    index index.html;

    access_log /var/log/nginx/kipphard.com.access.log;
    error_log  /var/log/nginx/kipphard.com.error.log;

    gzip on;
    gzip_types text/plain text/css text/javascript application/javascript application/json image/svg+xml;
    gzip_min_length 1024;

    # Custom 404 page emitted by vite-ssg as /404.html. Served (with 404 status)
    # on any try_files miss below.
    error_page 404 /404.html;

    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    location ~ \.html$ {
        add_header Cache-Control "no-cache, must-revalidate";
        try_files $uri =404;
    }

    location / {
        # vite-ssg emits flat files (e.g. /impressum.html, /blog.html). Try the
        # .html file BEFORE the directory ($uri/), so /blog resolves to blog.html
        # rather than being redirected into the /blog/ post directory (which has
        # no index). The $uri.html step also gives clean trailing-slash-less URLs.
        try_files $uri $uri.html $uri/ =404;
    }
}
